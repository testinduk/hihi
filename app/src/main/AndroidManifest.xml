<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Figma"
        tools:targetApi="31">
        <activity

            android:name=".main_home"
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity android:name=".notice_list"/>
        <activity android:name=".notice_writing"/>
        <activity android:name=".Timetable"/>
        <activity android:name=".chat_person"/>
        <activity android:name=".sharing_board"/>
        <activity android:name=".bullentin_board"/>
        <activity android:name=".bullentin_board_writing"/>
        <activity android:name=".mypage"/>
        <activity android:name=".bullentin_board_details"/>
        <activity android:name=".main_home"/>
        <activity android:name=".sharing_details"/>
        <activity android:name=".notice_details"/>


    </application>

</manifest>